/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    unoptimized: true, // optional — only if you want to disable Next.js image optimization
  },
};

module.exports = nextConfig;
